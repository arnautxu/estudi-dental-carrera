{
  "name": "estudi-dental-carrera",
  "private": true,
  "scripts": {
    "build": "node scripts/build-assets.cjs",
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
